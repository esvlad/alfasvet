{% if reviews %}
{% set r = 0 %}
{% for review in reviews %}
<div class="review">
  <div class="row">
    <div class="col-lg-2 col-sm-3 col-xs-5 review_author">
      <img src="catalog/view/theme/default/image/userpic.png">
      <p>{{ review.author }}</p>
    </div>
    <div class="col-lg-3 col-md-5 col-sm-4 col-xs-7 review_raiting">
      <div class="row">
        <div class="col-sm-8 col-xs-9">
          {% for i in 1..5 %}
            {% if review.rating < i %} 
              <span class="review_raiting_star"></span> 
            {% else %} 
              <span class="review_raiting_star _on"></span> 
            {% endif %}
          {% endfor %}
        </div>
        <div class="col-sm-3 col-xs-2">
          <p>{{ review.rating }}/5</p>
        </div>
      </div>
      <div class="row"></div>
    </div>
    <!--<div class="col-sm-4 review_special">
      <div class="row">
        <div class="col-sm-4">дизайн <b>+</b></div>
        <div class="col-sm-3">цена <b>-</b></div>
        <div class="col-sm-4">качество <b>+</b></div>
      </div>
    </div>-->
  </div>
  <div class="row review_text">
    <div class="col-sm-12"><p>{{ review.text }}</p></div>
  </div>
</div>
{% set r = r + 1 %}
{% endfor %}
<div class="pagination_result review_more clearfix">
  {% if r != total %}
  <div class="pagination_more">
    <div class="more_product" onclick="more_review();">Показать еще</div>
  </div>
  {% endif %}
  <div class="result_block">
    <p class="ptotals">Показано <span class="total_now">{{ r }}</span> из <span class="total">{{ total }}</span> отзывов</p>
  </div>
</div>
{% else %}
<p>Отзывы к данному товару отсутствуют, но вы можете оставить свой.</p>
{% endif %} 